% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_sets_registry.R
\name{validate_feature_sets}
\alias{validate_feature_sets}
\title{Validate Feature Set Names}
\usage{
validate_feature_sets(features, strict = TRUE)
}
\arguments{
\item{features}{Character vector of feature set names to validate}

\item{strict}{Logical; if TRUE (default), stops on unknown features.
If FALSE, warns and returns only valid features.}
}
\value{
Character vector of valid feature set names (invisibly)
}
\description{
Checks if requested feature sets are valid. By default, stops with an error
if unknown feature sets are found.
}
\examples{
# Valid features pass through
validate_feature_sets(c("stats", "acf"))

# Unknown features trigger error (strict=TRUE)
\dontrun{
validate_feature_sets(c("stats", "unknown"))
}

# With strict=FALSE, warns and returns valid features
validate_feature_sets(c("stats", "unknown"), strict = FALSE)
}
