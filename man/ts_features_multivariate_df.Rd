% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate_features.R
\name{ts_features_multivariate_df}
\alias{ts_features_multivariate_df}
\title{Extract multivariate features as data frame}
\usage{
ts_features_multivariate_df(X, features = "all", standardize = TRUE)
}
\arguments{
\item{X}{Either a matrix (N x T) or list of N numeric vectors}

\item{features}{Character vector of feature sets to extract (default "all")}

\item{standardize}{Logical; standardize series before extraction (default TRUE)}
}
\value{
A 1-row data.frame with one column per feature
}
\description{
Wrapper around ts_features_multivariate() that returns results as a 1-row
data frame. This format is convenient for batch processing of multiple
multivariate datasets.
}
\examples{
\dontrun{
# Extract features from multiple multivariate datasets
datasets <- list(
  system1 = matrix(rnorm(3 * 100), nrow = 3),
  system2 = matrix(rnorm(3 * 100), nrow = 3),
  system3 = matrix(rnorm(3 * 100), nrow = 3)
)

results <- do.call(rbind, lapply(datasets, ts_features_multivariate_df))
rownames(results) <- names(datasets)
}
}
