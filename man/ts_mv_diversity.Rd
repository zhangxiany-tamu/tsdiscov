% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate_diversity.R
\name{ts_mv_diversity}
\alias{ts_mv_diversity}
\title{Diversity and complexity features for multivariate time series}
\usage{
ts_mv_diversity(X, bins = 10, ...)
}
\arguments{
\item{X}{Matrix (N x T) where N is number of series, T is timepoints}

\item{bins}{Integer; number of bins for Shannon entropy computation (default 10)}

\item{...}{Additional arguments (currently ignored)}
}
\value{
Named list of 7 diversity/complexity features
}
\description{
Extracts features characterizing the diversity and complexity of the
multivariate system. Includes entropy-based measures and heterogeneity metrics.
}
\examples{
\dontrun{
# Heterogeneous system
X <- matrix(0, nrow = 4, ncol = 200)
X[1,] <- arima.sim(list(ar = 0.9), 200)  # Strong AR
X[2,] <- arima.sim(list(ar = 0.3), 200)  # Weak AR
X[3,] <- rnorm(200)  # White noise
X[4,] <- cumsum(rnorm(200))  # Random walk
diversity_features <- ts_mv_diversity(X)
}
}
