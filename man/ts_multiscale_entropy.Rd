% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_features.R
\name{ts_multiscale_entropy}
\alias{ts_multiscale_entropy}
\title{Extract Multi-scale Entropy features}
\usage{
ts_multiscale_entropy(x, scales = c(2, 5), m = 2, r = 0.2)
}
\arguments{
\item{x}{Numeric vector}

\item{scales}{Integer vector of scales (default: c(2, 5))}

\item{m}{Embedding dimension for sample entropy (default: 2)}

\item{r}{Tolerance fraction (default: 0.2)}
}
\value{
Named list with multi-scale entropy at each scale
}
\description{
Computes sample entropy at multiple temporal scales via coarse-graining.
Reveals complexity across different time scales.
}
\details{
At scale k, the series is coarse-grained by averaging non-overlapping windows
of size k, then sample entropy is computed on the coarse-grained series.
}
